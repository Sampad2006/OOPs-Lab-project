--extra-index-url https://download.pytorch.org/whl/cpu
# --- Backend Framework ---
fastapi>=0.100.0         # Modern, fast web framework for building APIs
uvicorn>=0.22.0          # ASGI web server implementation for FastAPI
python-multipart>=0.0.6  # Required by FastAPI for handling File/Form uploads

# --- Computer Vision & OCR ---
pytesseract>=0.3.10      # Python wrapper for Tesseract OCR (requires Tesseract installed on OS)
easyocr>=1.7.0           # PyTorch-based OCR, used as fallback for handwritten text
pymupdf>=1.24.0          # PDF document rendering and processing
Pillow>=10.0.0           # Image processing and manipulation
opencv-python-headless>=4.8.0 # Computer vision utilities (bounding boxes, layout analysis)

# --- Cloud Extraction APIs ---
google-genai>=0.3.0      # Google's latest SDK for Gemini 2.0+ Vision extraction
groq>=1.2.0              # Groq SDK for ultra-fast Llama Vision models

# --- Machine Learning & Evaluation ---
sentence-transformers>=2.2.0 # Deep learning models for semantic embedding similarity
scikit-learn>=1.3.0      # Used for TF-IDF similarity computation
python-Levenshtein>=0.21.0 # Fast computation of Edit Distance (Levenshtein)
transformers>=4.35.0     # HuggingFace transformers (used by local ML models and embeddings)
torch>=2.1.0             # PyTorch backend for EasyOCR and sentence-transformers
numpy>=1.24.0            # Core array computation library
pandas>=2.0.0            # Data manipulation (used in similarity aggregation logging)

# --- Legacy (Optional) ---
streamlit>=1.30.0        # Deprecated: Kept for backwards compatibility with legacy app.py